# Shell scripts must always check out with LF line endings, regardless of the
# cloning machine's autocrlf setting - a CRLF shell script fails immediately on Linux
# (Railway, GitHub Actions) with `$'\r': command not found`. live/scripts/smoke_test.sh
# specifically needs to run correctly there.
*.sh text eol=lf
